# ==============================================================================
# DOCSLY - ENVIRONMENT CONFIGURATION TEMPLATE
# ==============================================================================
# Path: src/.env.example
# Copy this file to .env and fill in the required values.
# ==============================================================================

# --- APPLICATION METADATA ---
APP_NAME="Docsly"
APP_VERSION="0.1"

# --- INFRASTRUCTURE: MONGODB ---
MONGODB_USERNAME="admin"
MONGODB_PASSWORD="admin_password_here"
# Connection string for the MongoDB instance (Local Docker or Remote Atlas)
# Format: mongodb://[user:pass@]host:port/
MONGODB_URL="mongodb://${MONGODB_USERNAME}:${MONGODB_PASSWORD}@localhost:27017/"
MONGODB_DATABASE_NAME="docsly_rag_db"

# --- AI & LLM PROVIDERS ---
# OpenAI API Key for RAG functionality and Embeddings
OPENAI_API_KEY="your-openai-api-key-here"

# --- FILE UPLOAD & PROCESSING ---
# Allowed MIME types for ingestion (JSON list format)
FILE_ALLOWED_TYPES=["application/pdf", "text/plain", "application/vnd.openxmlformats-officedocument.wordprocessingml.document"]

# Maximum file size allowed for upload (in bytes) - Default: 10MB
FILE_MAX_SIZE=10485760

# Internal chunk size for file streaming (in bytes) - Default: 512KB
FILE_DEFAULT_CHUNK_SIZE=524288

# --- END OF CONFIGURATION TEMPLATE ---